CREATE TABLE  D_USERINFO 
   (	NUM NUMBER(30,0) NOT NULL ENABLE, 
	NAME VARCHAR2(100) NOT NULL ENABLE, 
	WRITER VARCHAR2(100) NOT NULL ENABLE, 
	PUBLISHER VARCHAR2(100) NOT NULL ENABLE, 
	AREA VARCHAR2(100) NOT NULL ENABLE, 
	STATE VARCHAR2(100) NOT NULL ENABLE, 
	ID VARCHAR2(100) NOT NULL ENABLE, 
	UDATE DATE NOT NULL ENABLE
   ) ;



CREATE TABLE  D_REQUESTBOOK 
   (	NUM NUMBER(10,0) NOT NULL ENABLE, 
	RID VARCHAR2(100) NOT NULL ENABLE, 
	RNAME VARCHAR2(100) NOT NULL ENABLE, 
	RWRITER VARCHAR2(100) NOT NULL ENABLE, 
	RPUBLISHER VARCHAR2(100) NOT NULL ENABLE, 
	RDATE DATE NOT NULL ENABLE, 
	 CONSTRAINT D_REQUESTBOOK_PK PRIMARY KEY (NUM) ENABLE
   ) ;




CREATE TABLE  D_QNA 
   (	NUM NUMBER(10,0) NOT NULL ENABLE, 
	TITLE VARCHAR2(300) NOT NULL ENABLE, 
	PASSWORD NUMBER(4,0) NOT NULL ENABLE, 
	CONTENT VARCHAR2(2500) NOT NULL ENABLE, 
	COMMAND VARCHAR2(2500), 
	QDATE DATE NOT NULL ENABLE, 
	 CONSTRAINT D_QNA_PK PRIMARY KEY (NUM) ENABLE
   ) ;





 
CREATE TABLE  D_PRINT 
   (	PRINT_NUM NUMBER(10,0) NOT NULL ENABLE, 
	PRINT_SENDER VARCHAR2(700) NOT NULL ENABLE, 
	PRINT_TITLE VARCHAR2(900) NOT NULL ENABLE, 
	PRINT_FILE VARCHAR2(1000) NOT NULL ENABLE, 
	PRINT_FILE_SIZE VARCHAR2(4000) NOT NULL ENABLE, 
	PRINT_CONTENTS VARCHAR2(4000), 
	PRINT_DATE DATE NOT NULL ENABLE, 
	 CONSTRAINT D_PRINT_PK PRIMARY KEY (PRINT_NUM) ENABLE
   ) ;


 
CREATE TABLE  D_NOTICE 
   (	NUM NUMBER(30,0) NOT NULL ENABLE, 
	PEOPLE VARCHAR2(100) NOT NULL ENABLE, 
	TITLE VARCHAR2(500) NOT NULL ENABLE, 
	CONTENT VARCHAR2(2500) NOT NULL ENABLE, 
	DDATE DATE NOT NULL ENABLE, 
	 CONSTRAINT D_NOTICE_PK PRIMARY KEY (NUM) ENABLE
   ) ;




CREATE TABLE  D_MONEY 
   (	NUM NUMBER(15,0) NOT NULL ENABLE, 
	ID VARCHAR2(100) NOT NULL ENABLE, 
	MONEY NUMBER(15,0) NOT NULL ENABLE, 
	TOTAL NUMBER(15,0) NOT NULL ENABLE, 
	RECHARGE NUMBER(15,0) NOT NULL ENABLE, 
	 CONSTRAINT D_MONEY_PK PRIMARY KEY (NUM) ENABLE
   ) ;




CREATE TABLE  D_MEMBER 
   (	NUM NUMBER(10,0) NOT NULL ENABLE, 
	MEM_ID VARCHAR2(50) NOT NULL ENABLE, 
	MEM_PW VARCHAR2(50) NOT NULL ENABLE, 
	MEM_PW_CHECK_QUESTION VARCHAR2(900) NOT NULL ENABLE, 
	MEM_PW_CHECK_ANSWER VARCHAR2(900) NOT NULL ENABLE, 
	MEM_NAME VARCHAR2(70) NOT NULL ENABLE, 
	MEM_ADDRESS VARCHAR2(200) NOT NULL ENABLE, 
	MEM_TEL VARCHAR2(200), 
	MEM_PHONE VARCHAR2(200) NOT NULL ENABLE, 
	MEM_EMAIL VARCHAR2(200) NOT NULL ENABLE, 
	MEM_JOIN_DATE DATE NOT NULL ENABLE, 
	MEM_POINT NUMBER(10,0) NOT NULL ENABLE, 
	 CONSTRAINT D_MEMBER_PK PRIMARY KEY (MEM_ID) ENABLE, 
	 CONSTRAINT D_MEMBER_UK1 UNIQUE (MEM_PHONE) ENABLE
   ) ;







 
CREATE TABLE  D_BOOK 
   (	NUM NUMBER(10,0) NOT NULL ENABLE, 
	NAME VARCHAR2(100) NOT NULL ENABLE, 
	WRITER VARCHAR2(100) NOT NULL ENABLE, 
	PUBLISHER VARCHAR2(100) NOT NULL ENABLE, 
	AREA VARCHAR2(100) NOT NULL ENABLE, 
	COUNT NUMBER(10,0) NOT NULL ENABLE, 
	IMGPATH VARCHAR2(1000), 
	POPULAR NUMBER(10,0), 
	 CONSTRAINT D_BOOK_PK PRIMARY KEY (NUM) ENABLE
   ) ;







CREATE TABLE  D_BOARD 
   (	NUM NUMBER(30,0) NOT NULL ENABLE, 
	PEOPLE VARCHAR2(100) NOT NULL ENABLE, 
	TITLE VARCHAR2(300) NOT NULL ENABLE, 
	CONTENT VARCHAR2(2500) NOT NULL ENABLE, 
	BDATE DATE NOT NULL ENABLE, 
	 CONSTRAINT D_BOARD_PK PRIMARY KEY (NUM) ENABLE
   ) ;





CREATE TABLE  D_ADMIN 
   (	ADMIN_NUM NUMBER(1,0) NOT NULL ENABLE, 
	ADMIN_ID VARCHAR2(40) NOT NULL ENABLE, 
	ADMIN_PW VARCHAR2(80) NOT NULL ENABLE, 
	ADMIN_NAME VARCHAR2(130) NOT NULL ENABLE, 
	 CONSTRAINT D_ADMIN_PK PRIMARY KEY (ADMIN_ID, ADMIN_NAME) ENABLE
   ) ;





